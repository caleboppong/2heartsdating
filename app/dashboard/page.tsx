"use client";
import { useEffect, useState } from "react";import Link from "next/link";import { useRouter } from "next/navigation";import { supabase } from "@/lib/supabaseClient";
export default function DashboardPage(){const router=useRouter();const[email,setEmail]=useState("");const[role,setRole]=useState("user");useEffect(()=>{async function loadUser(){const{data}=await supabase.auth.getUser();if(!data.user){router.push('/login');return}setEmail(data.user.email||"");const{data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).single();setRole(profile?.role||'user')}loadUser()},[router]);async function logout(){await supabase.auth.signOut();router.push('/login')}return <main className="min-h-screen bg-pink-50 p-6"><div className="mx-auto max-w-6xl"><div className="rounded-2xl bg-white p-8 shadow"><h1 className="text-4xl font-bold text-[#101B3D]">Dashboard</h1><p className="mt-2 text-slate-600">Logged in as: {email}</p>{role==='admin'&&<p className="mt-2 font-semibold text-pink-600">Admin account</p>}<div className="mt-8 grid gap-5 md:grid-cols-3"><Link href="/profile/edit" className="rounded-xl border bg-white p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold">Edit Profile</h2><p className="mt-2 text-slate-600">Complete your personal dating profile.</p></Link><Link href="/profile/photos" className="rounded-xl border bg-white p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold">Upload Photos</h2><p className="mt-2 text-slate-600">Add profile and gallery pictures.</p></Link><Link href="/search" className="rounded-xl border bg-white p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold">Find Matches</h2><p className="mt-2 text-slate-600">Browse approved members.</p></Link><Link href="/matches" className="rounded-xl border bg-white p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold">Matches & Chat</h2><p className="mt-2 text-slate-600">Chat with matched users.</p></Link><Link href="/subscription" className="rounded-xl border bg-white p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold">Subscription</h2><p className="mt-2 text-slate-600">Manage your membership.</p></Link>{role==='admin'&&<Link href="/admin" className="rounded-xl border bg-pink-50 p-6 shadow hover:border-pink-500"><h2 className="text-xl font-bold text-pink-600">Admin Dashboard</h2><p className="mt-2 text-slate-600">Approve users and manage the platform.</p></Link>}</div><button onClick={logout} className="mt-8 rounded-xl bg-[#101B3D] px-6 py-3 font-semibold text-white">Logout</button></div></div></main>}
