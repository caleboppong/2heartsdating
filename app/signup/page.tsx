"use client";
import { useState } from "react";
import Link from "next/link";
import { supabase } from "@/lib/supabaseClient";
export default function SignupPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[message,setMessage]=useState("");async function handleSignup(e:React.FormEvent){e.preventDefault();setMessage("Creating account...");const{error}=await supabase.auth.signUp({email,password});if(error){setMessage(error.message);return}setMessage("Account created. Please check your email to confirm your account.")}return <main className="flex min-h-screen items-center justify-center bg-pink-50 px-4"><form onSubmit={handleSignup} className="w-full max-w-md rounded-2xl bg-white p-8 shadow"><h1 className="text-3xl font-bold text-[#101B3D]">Create Account</h1><p className="mb-6 mt-2 text-slate-600">Join 2heartsdating today.</p><input className="mb-4 w-full rounded-xl border px-4 py-3" type="email" placeholder="Email address" value={email} onChange={(e)=>setEmail(e.target.value)} required/><input className="mb-4 w-full rounded-xl border px-4 py-3" type="password" placeholder="Password" value={password} onChange={(e)=>setPassword(e.target.value)} required minLength={6}/><button className="w-full rounded-xl bg-pink-600 py-3 font-semibold text-white">Sign Up</button>{message&&<p className="mt-4 text-sm text-slate-700">{message}</p>}<p className="mt-6 text-sm">Already have an account? <Link href="/login" className="font-semibold text-pink-600">Login</Link></p></form></main>}
